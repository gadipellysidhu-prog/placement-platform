/**
 * Base URL used by MSW request handlers. Must match the `VITE_API_BASE_URL`
 * injected by vitest.config.ts so intercepted URLs line up with the axios baseURL.
 */
export const API_BASE_URL = 'http://localhost:8081'
